name: Release Drafter

on:
  push:
    branches: [ main ]

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: release-drafter/release-drafter@1eb031648ba18ffa376d3e4f2c33a6364a5a4dc2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
