name: Release Drafter

on:
  push:
    branches: [ main ]

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: release-drafter/release-drafter@45b321a77828485e6f7ad078c5bb182f72a4c581
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
